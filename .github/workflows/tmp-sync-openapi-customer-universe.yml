name: Temporary OpenAPI Sync Customer Universe

on:
  push:
    branches:
      - feat/customer-universe-professional-v1
    paths:
      - "koschei/api/internal/http/**"
      - "koschei/api/openapi.yaml"
      - ".github/workflows/tmp-sync-openapi-customer-universe.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-openapi:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/customer-universe-professional-v1
          fetch-depth: 0
      - uses: actions/setup-go@v7
        with:
          go-version-file: koschei/api/go.mod
          cache-dependency-path: koschei/api/go.sum
      - name: Regenerate exact OpenAPI contract
        working-directory: koschei/api
        run: go run ./cmd/openapi-gen
      - name: Commit generated contract when changed
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- koschei/api/openapi.yaml; then
            echo "OpenAPI already synchronized."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add koschei/api/openapi.yaml
          git commit -m "chore(openapi): sync Professional customer route contract"
          git push origin HEAD:feat/customer-universe-professional-v1
